#pragma once
#include <godot_cpp/core/class_db.hpp>

using namespace godot;

void initialize_distance_matrix_module(ModuleInitializationLevel p_level);
void uninitialize_distance_matrix_module(ModuleInitializationLevel p_level);